# Create Masterprompt — 中文版

> `SKILL.md` 的译文。**程序读取的只有 `SKILL.md`**，即英文原件——这份
> 中文版供人阅读参考。两者若有出入，以英文版为准。对应版本：1.7.0。
>
> 文件名、目录和示例命令有意保留原文，因为它们在硬盘上就叫这个名字。
>
> 一页看全貌，不必通读：[`docs/uebersicht-zh.png`](docs/uebersicht-zh.png)
> （英文版：[`docs/uebersicht-en.png`](docs/uebersicht-en.png)）。
>
> **现状：早期发布。** 文本已多次检查内部一致性和规范符合度，但这个 Skill
> 本身还没有在很多真实、各异的项目上跑过。如果流程、规模分级或某个模板与你
> 自己的工作方式不合，那是一个有用的信号——欢迎提 Issue。

Masterprompt **不是**角色提示词。“你是一名资深工程师，请做事周全”并不能让
一个有能力的模型多做任何它本来不会做的事。Masterprompt 是一个**上下文包**：
一个项目里持久的事实、决定和边界，写得让一个毫无历史的会话能恰好从上一个
会话停下的地方接着做。

这个 Skill 分六个阶段构建这个包，最后交回三个文件。

## 产出什么

| 文件 | 用途 | 有效期 |
|---|---|---|
| `BRIEFING.md` | Masterprompt 本身。上下文、决定、排除范围、陷阱、当前状态。 | 整个项目 |
| `DECISIONS.md` | 每个已定问题一行，附理由。只追加，不覆盖。 | 整个项目 |
| `HANDOFF_vNN.md` | 在上下文上限之前写。一个新会话*此刻*需要的东西。 | 一个会话 |

按用户的语言给它们命名。放在工作成果旁边，而不是留在聊天里。

## 阶段 0 — 规模分级（先做这个，十秒钟）

对一个改名脚本跑完六个阶段，正是人们学会干脆跳过整个流程的方式。开始之前
先分类：

- **S——一次坐下就能完成，可逆，没有未知数。** 直接干活。只有当它长大了，
  才提议走完整流程。
- **M——几个会话，有些未知数，一两个真正的分叉。** 阶段 1–3 和 5，工作一旦
  超出一个会话就加上 6。有 Briefing 文件，但要短。不单独写计划文档。
- **L——跨多个会话，真正的架构，撤回代价高昂的决定。** 全部六个阶段。

用一句话说明你选了哪个规模、为什么。用户不同意会自己说——这花一条消息，
省一个小时。

## 阶段 1 — 调研

弄清已经有什么、缺口在哪里、技术上什么可行、哪些陷阱已有记录。**调研只在
这里进行。** 边构建边调研，就是构建变成兔子洞的方式。

交付物：一份报告，含对比表、一条建议和来源。

**三条全部成立时才停下**——而不是好奇心耗尽时：
1. 入围选项在对比表里没有空单元格。
2. 每一条陷阱都有来源，或标为假设。
3. 最后两次搜索没有带来新东西。这就是饱和。

达不到饱和就直说，并指出哪些还没弄清。一个诚实的缺口胜过一个自信的猜测，
而且用户可以自己决定要不要再花时间。

## 阶段 2 — Briefing

用 `assets/template-briefing.md` 把调研压缩成 `BRIEFING.md`。

这份文件的检验标准：**把它交给一个没有任何历史的新会话，它能干活吗？**
如果它还得就你已经知道的事问哪怕一个澄清问题，Briefing 就不完整。拿出来
之前，先用挑刺的眼光回读一遍。

必选章节——前两个正是人们跳过然后后悔的：

- **排除范围（Anti-scope）。** 明确的非目标，每条附理由。“v1 不做
  加密——保险库只在本地，密钥管理会让工作量翻倍。”排除范围是对付范围蔓延最有力
  的防线，因为它把“我们能不能顺便……”变成一个必须重新打开的决定，而不是
  一个免费的附加项。
- **假设登记表。** 所有你没问就定下的事。每条一行，做上标记，方便日后质疑。
  没记下来的假设是看不见的，直到它变得昂贵。
- 上下文、约束、已知陷阱、当前状态。

## 阶段 3 — 决策访谈

把待定的决定**一次问一个**，等到回答之后再问下一个。打包的问题会被一扫而过，
而被一扫而过的决定，是一个盖着用户签名的猜测。

每个问题：2–4 个选项，一条明确的建议，以及建议的理由。按顺序解决
依赖——先定技术栈，再定跑在它上面的库。

**自己去查。** 凡是能从文件、工具或网络查到的事实，就自己查。只有真正的
选择才属于用户。

**这个阶段没有收尾之前，什么都不构建。** 如果用户在访谈中途说“直接开始
吧”：点名还没定的具体决定，提议由你自己把它们定下来并记录为假设，等用户
选了再继续。带着未决分叉开工意味着返工，而返工的代价比访谈更高。

最后给出所有决定的编号摘要。把它追加到决策日志里，决策日志遵循
`assets/template-decisions.md`。

## 阶段 4 — 计划

架构、仓库或目录结构、约定文件、测试策略、里程碑、每个里程碑的完成定义，
以及整个项目的完成定义。

里程碑是**纵向切片**：每一个都产出用户真的能运行、看到或使用的东西。五个
各交付一小片可用功能的里程碑，胜过三个只交付一个谁也测不了的地基的里程碑。

构建之前先把计划拿出来审批。

## 阶段 5 — 构建

对着一个明确说出的目标干活，目标带有**可检验的停止条件**。“`npm test`
通过且应用能打开保险库目录即为完成”是可检验的。“运行良好即为完成”不是。

- 测试有意义的地方，先写测试再写功能。
- 小的 Commit，每个都能单独回滚。
- Linting 做成 Hook，而不是靠提醒。
- 不为请求许可而停下。只在真正的设计分叉处才问。

## 阶段 6 — 交接

在上下文上限之前——不是之后——根据 `assets/template-handoff.md` 写交接文件
（`HANDOFF_vNN.md`，按用户的语言命名），然后开一个新会话。没完没了的压缩
丢掉的，恰恰是那些花了大代价才确定下来的细节。

触发信号：长时间大量调用工具，反复重读同样的文件，或者用户第二次问起已经
讲过的东西。

## 改进环节

在每个阶段边界，在展示结果**之前**，悄悄从写作者切换成审阅者。三个问题：

1. **缺了什么？** 一个新会话不得不问、而这份文件没有回答的问题是哪个？
2. **哪些是断言而不是证据？** 每一条没有来源或假设标记的说法都是候选。
3. **哪些是填充物？** 任何一句放到别的项目里也能用的话，都删掉。

用户看到的是修改后的结果，而不是批评。例外：如果这一环节发现了触及某个
决定的东西，那就得摆到用户面前。

产出和评估是两种不同的活动。写作者看不见缺口，因为缺的那一块在他自己脑子
里。这大约花掉该阶段 30 % 的工夫，回报却比这个 Skill 里的任何其他东西都高。

当任务形态超出“给我写个 X”时，先加载 `references/prompt-techniques.md`——那里
有从任务形态到技术手法的路由表，以及过度提示的警示信号。

## 回退规则

决定会被修改。这很正常，代价也低，**只要处理得当**：

1. 更新决策日志——加新的一行，把旧的一行标为已被取代，两行都留着。历史
   解释了代码为什么长成这样。
2. 更新 `BRIEFING.md`，因为新会话读的就是它。
3. 动代码之前，先说清这次改动让什么失效了。

只在聊天里改变方向，就是故障模式：文件仍在描述旧项目，下一个会话相信文件，
矛盾在三步之后才浮出水面。

## 退出规则

一个三个会话下来毫无产出的项目，要么卡住了，要么已经死了。直说，并给出
三个选项：缩小范围；写好交接后搁置，以便日后干净地重启；或者放弃。想法
很便宜，半成品项目却要维护。这是唯一一个直言不讳本身就是服务的地方。

## 易踩的坑

与合理假设相悖的环境事实。绕过某个症状之前，先查这里。

- **Anthropic 的 Skill Frontmatter 恰好只接受六个键**：`name`、
  `description`、`license`、`allowed-tools`、`metadata`、`compatibility`。
  其他任何键上传到 claude.ai 时都通不过校验，尽管 Claude Code 能容忍。
  Skill 在本地悄无声息地正常工作，一上传就坏。
- **目录名必须等于 `name` 字段**，经 NFKC 规范化之后。改了目录名却没改
  Frontmatter，是最常见的故障。
- **`name`：只允许小写字母、数字和连字符。** 不能有下划线，不能有连续的
  连字符，开头和结尾不能是连字符。最多 64 个字符。`description` 最多 1024。
- **`description` 里没加引号的冒号会破坏 YAML 解析。** “Use when: …”会
  失败。加引号，或者用块标量（`>-`）。
- **会话开始时只加载 `name` 和 `description`。** 正文在触发时才加载。
  所以每一条“何时使用”的提示都得放在 description 里；埋在正文里的触发
  条件永远来不及被读到。
- **简单的单步请求不会触发 Skill**，无论描述写得多好，因为模型会直接处理
  它们。用有分量的、多步骤的提示来测试触发。
- **Skill 是指令，不是强制。** `allowed-tools` 免除了权限询问；它不限制
  任何东西。

## 参考文件

阶段需要时再加载，不要一上来就全加载：

- `references/profile-questionnaire.md`——用户只需填一次、就能让这个 Skill
  变成自己的那些填空项。首次使用时读，或者用户想要个人定制版时读。
- `references/quality-gates.md`——每个阶段的检查清单。收尾任何阶段之前读。
- `references/anti-patterns.md`——故障模式及其修复方法。项目停滞、原地打转
  或产生返工时读。
- `references/model-routing.md`——哪一档模型适合哪个阶段。用户在意模型选择
  或成本时读。
- `references/prompt-techniques.md`——从任务形态到技术手法的路由，外加过度
  提示的警示信号。某个阶段交不出东西、某份产物显得单薄，或者任务超出“给我
  写个 X”时读。

`assets/` 下的模板是拿来复制并填写的，不是拿来改写的。结构比对结构的散文
描述更容易被可靠地匹配上。

## 工作准则

只要这个 Skill 处于激活状态，这些规则塑造每一次回答：

- **以用户的语言作答**，包括生成的文件。这个 Skill 是用英文写的；输出不是。
- **每次回答附一小段学习要点。** 两到四句话，讲*为什么*，不讲是什么。目的
  是让用户不用你也能跑下一个项目。
- **模型选择上，质量优先于省钱。** 当更强的模型能给出更好的结果时，绝不
  降级到更便宜的模型。只有结果相当时，更便宜才是对的。拿不准时，留在高档
  并说明。
- **凡是出错代价高的地方，绝不猜。** 回复里的一个错别字不花什么。文件名、
  标识符、数据格式或 Commit 里的一个错别字要花一个下午。正确版本显而易见
  时悄悄改正；不明显时就问。
- **不清楚的缩写：问，别猜。** 如果有常见的展开，就点出来。这里猜错会
  滚雪球——三步之后它已经承重，拆起来代价高昂。
- **把假设说出来。** 如果你不确定自己的假设成立，就说出来，而不是当作事实
  来兜售。
- **任务完成后不留尾巴问题。** 没有“还需要别的吗？”，没有收尾的选项菜单。
  用户想要更多会自己说。
