# Create Masterprompt — 日本語版

> `SKILL.md` の翻訳。**プログラムが読むのは英語のファイルである `SKILL.md`
> だけ**で、こちらは人が読むためのものだ。両者に食い違いがあれば英語版が
> 優先する。バージョン 1.7.0 時点の内容。
>
> ファイル名、フォルダ名、コマンド例は意図的に翻訳していない。ディスク上で
> その名前になっているからだ。
>
> 通読する代わりに一枚で見渡すには：[`docs/uebersicht-ja.png`](docs/uebersicht-ja.png)
> （英語版：[`docs/uebersicht-en.png`](docs/uebersicht-en.png)）。
>
> **状況：早期公開。** 本文は内部に矛盾がないことと仕様への適合を何度も
> 検証済みだが、スキル自体はまだ多くの実際の、種類の異なる案件で使われて
> いない。手順、サイズゲート、テンプレートのどこかが自分の仕事の進め方に
> 合わないなら、それは有用なシグナルだ — Issue として報告してほしい。

マスタープロンプトはロールプロンプト**ではない**。「あなたはシニアエンジニア
です、丁寧に作業してください」は、有能なモデルがすでにやっていること以上の
何も加えない。マスタープロンプトとは**コンテキストパッケージ**だ：ひとつの
プロジェクトの持続的な事実、決定、境界を、履歴ゼロのセッションが前回止まった
まさにその場所から再開できるように書いたもの。

このスキルは六つのフェーズでそのパッケージを組み立て、三つのファイルを返す。

## 成果物

| ファイル | 役割 | 有効期間 |
|---|---|---|
| `BRIEFING.md` | マスタープロンプト本体。コンテキスト、決定事項、アンチスコープ、落とし穴、現状。 | プロジェクト全体 |
| `DECISIONS.md` | 決着した問いごとに一行、理由付き。追記のみ。 | プロジェクト全体 |
| `HANDOFF_vNN.md` | コンテキスト上限の前に書く。新しいセッションが*いま*必要とするもの。 | セッションひとつ |

ファイル名はユーザーの言語で付ける。チャットの中ではなく、作業のすぐ隣に
置く。

## フェーズ 0 — サイズゲート（最初に、十秒で）

リネームスクリプトに六つのフェーズを走らせるのは、人がプロセスを丸ごと
飛ばすことを覚える道筋だ。始める前に分類する：

- **S — 一回で終わり、元に戻せて、未知の要素がない。** そのまま作業に取り
  かかる。大きくなった場合にだけ、フルの手順を提案する。
- **M — 数セッション、いくつかの未知、本物の分岐が一つか二つ。** フェーズ
  1–3 と 5、それに作業が一セッションを超えた時点で 6。ブリーフィングファイル
  は作るが、短く。独立した計画文書は作らない。
- **L — 複数セッション、本物のアーキテクチャ、取り消すのが高くつく決定。**
  六つのフェーズすべて。

どのサイズを選び、なぜかを一文で言う。ユーザーが違う意見なら、そう言って
くる — メッセージ一通のコストで一時間が浮く。

## フェーズ 1 — リサーチ

すでに何が存在し、どこに隙間があり、技術的に何が可能で、どの落とし穴が
すでに文書化されているかを調べる。**リサーチはここでだけ行う。** ビルドの
途中でのリサーチは、ビルドを底なし沼に変える道筋だ。

成果物：比較表、推奨案、出典を備えたレポート。

**三つがすべて満たされたら止める** — 好奇心が尽きたときではない：
1. 最終候補について、比較表に空欄がない。
2. 落とし穴の各行に出典があるか、仮定として印が付いている。
3. 直近二回の検索で新しいものが出なかった。それが飽和だ。

飽和に達せられないなら、そう言い、何が未解決のまま残ったかを挙げる。
正直な隙間は自信ありげな推測に勝つし、さらに時間をかけるかどうかは
ユーザーが決められる。

## フェーズ 2 — ブリーフィング

`assets/template-briefing.md` を使って、リサーチを `BRIEFING.md` に圧縮する。

このファイルのテスト：**履歴のない新しいセッションに渡す。それで作業
できるか？** すでに知っていたことについて確認の質問が一つでも必要なら、
ブリーフィングは不完全だ。見せる前に、敵対的な目で読み返す。

必須セクション — 最初の二つは、人が飛ばして後悔するものだ：

- **アンチスコープ。** 明示的な非目標を、それぞれ理由付きで。「v1 では
  暗号化なし — 保管庫はローカル限定で、鍵管理を入れるとビルドが倍になる。」
  アンチスコープはスコープクリープに対して使える最強の防御だ。「ちょっと
  これも……」を、無料の追加ではなく、あらためて決め直す必要のある決定に
  変えるからだ。
- **仮定の台帳。** 尋ねずに決めたことすべて。一行ずつ、後で異議を唱えられる
  ように印を付けて。記録されていない仮定は、高くつくまで見えない。
- コンテキスト、制約、既知の落とし穴、現状。

## フェーズ 3 — 決定インタビュー

未決の決定を**一つずつ**尋ね、次に進む前に答えを待つ。まとめて出した質問は
流し読みされ、流し読みされた決定は、ユーザーの署名をまとった推測だ。

質問ごとに：2–4 の選択肢、明確な推奨案一つ、その理由。依存関係は順に
解決する — その上で動くライブラリより先にスタックを決める。

**自分で調べる。** ファイル、ツール、Web から見つけられる事実は、自分で
見つける。ユーザーに委ねるのは本物の選択だけだ。

**このフェーズが閉じる前には何も作らない。** インタビューの途中でユーザーが
「とにかく始めて」と言ったら：まだ開いている具体的な決定を挙げ、記録済みの
仮定として自分で決めることを申し出て、ユーザーが選んでから初めて続ける。
分岐を開いたまま始めれば手戻りになり、手戻りはインタビューより高くつく。

締めくくりに、すべての決定の番号付きサマリー。`assets/template-decisions.md`
に従う決定ログに追記する。

## フェーズ 4 — 計画

アーキテクチャ、リポジトリまたはフォルダ構成、規約ファイル、テスト戦略、
マイルストーン、マイルストーンごとの完了の定義、そしてプロジェクト全体と
しての完了の定義。

マイルストーンは**垂直スライス**だ：それぞれが、ユーザーが実際に動かし、
見て、使えるものを生む。動く一片ずつを届ける五つのマイルストーンは、
誰もテストできない土台を届ける三つに勝る。

作り始める前に、計画を見せて承認を得る。

## フェーズ 5 — ビルド

**検証可能な停止条件**を持つ、明言された目標に向かって作業する。「`npm test`
が通り、アプリが保管庫フォルダを開いたら完了」は検証可能だ。「うまく動いた
ら完了」はそうではない。

- テストに意味がある箇所では、機能より先にテスト。
- 小さなコミット、それぞれ単独で戻せるように。
- リントはフックで、リマインダーではなく。
- 許可を求めて止まらない。尋ねるのは本物の設計上の分岐だけ。

## フェーズ 6 — ハンドオフ

コンテキスト上限の前に — 後ではなく — `assets/template-handoff.md` から
ハンドオフファイル（`HANDOFF_vNN.md`、ユーザーの言語で命名）を書き、その
うえで新しいセッションを始める。際限のないコンパクションは、確立するのに
高くついた細部をまさに失わせる。

トリガー：ツール中心の長い区間、同じファイルの繰り返しの読み直し、あるいは
すでに扱ったことをユーザーが二度尋ねること。

## 改善パス

すべてのフェーズ境界で、結果を見せる**前に**、静かに書き手からレビュアーに
切り替える。三つの問い：

1. **何が欠けているか？** 新しいセッションが尋ねざるを得ない問いのうち、
   このファイルが答えていないものはどれか？
2. **何が根拠なしに断言されているか？** 出典も仮定の印もない主張はすべて
   候補だ。
3. **何が埋め草か？** 他のどのプロジェクトにも出てきうる文は削る。

ユーザーが見るのは修正後の結果であって、批評ではない。例外：パスが決定に
関わる何かを浮かび上がらせたら、それはユーザーの前に出す。

作ることと評価することは別の活動だ。書き手には隙間が見えない。欠けている
部品が自分の頭の中にあるからだ。フェーズのおよそ 30 % のコストがかかり、
このスキルの他のどれよりも多くを返す。

タスクの形が「X を書いて」を超えるときは、まず `references/prompt-techniques.md`
を読み込む — タスクの形から技法への振り分け表と、過剰プロンプトの警告
サインが載っている。

## 巻き戻しルール

決定は見直される。それは普通のことで、**正しく扱えば**安上がりだ：

1. 決定ログを更新する — 新しい行を足し、古い行に置き換え済みの印を付け、
   両方を残す。履歴が、コードがなぜそういう姿なのかを説明する。
2. `BRIEFING.md` を更新する。新しいセッションが読むのはそれだからだ。
3. コードに触れる前に、その変更が何を無効にするかを挙げる。

チャットの中だけで方向転換するのが失敗パターンだ：ファイルは古いプロジェクト
を記述したままで、次のセッションはそれを信じ、矛盾は三歩先で表面化する。

## 撤退ルール

三セッションで何も生み出していないプロジェクトは、行き詰まっているか死んで
いる。それを率直に言い、三つの選択肢を出す：スコープを縮める、きれいに再開
できるよう書面のハンドオフを付けて保留する、あるいは捨てる。アイデアは安い
が、作りかけのプロジェクトには維持費がかかる。ここだけは、遠慮のなさが役に
立つ場面だ。

## ハマりどころ

合理的な想定に反する環境の事実。症状を回避する前にここを確認する。

- **Anthropic のスキル用フロントマターが受け付けるキーはちょうど六つ**：
  `name`、`description`、`license`、`allowed-tools`、`metadata`、
  `compatibility`。それ以外は、Claude Code が許容していても、claude.ai への
  アップロード時に検証で落ちる。スキルはローカルでは黙って動き、アップロード
  で壊れる。
- **フォルダ名は NFKC 正規化後の `name` フィールドと一致していなければ
  ならない。** フロントマターを直さずにフォルダをリネームするのが、最もよく
  ある壊れ方だ。
- **`name`：小文字、数字、ハイフンのみ。** アンダースコア不可、ハイフンの
  連続不可、先頭・末尾のハイフン不可。最大 64 文字。`description` は最大
  1024。
- **`description` 内のクォートされていないコロンは YAML のパースを壊す。**
  「Use when: …」は失敗する。クォートするか、ブロックスカラー（`>-`）を使う。
- **セッション開始時に読み込まれるのは `name` と `description` だけ。** 本文は
  トリガー時に読み込まれる。だから「いつ使うか」のヒントはすべて description
  に置く。本文に埋もれたトリガー条件は、トリガーに間に合うように読まれる
  ことは決してない。
- **単純な一段階の依頼はスキルをトリガーしない。** description の出来に
  かかわらず、モデルが直接処理するからだ。トリガーのテストは、中身のある
  複数ステップのプロンプトで行う。
- **スキルは指示であって、強制ではない。** `allowed-tools` は権限確認を
  免除するだけで、何も制限しない。

## リファレンスファイル

フェーズが必要とするときに読み込む。前もってではない：

- `references/profile-questionnaire.md` — このスキルを自分のものにするために
  ユーザーが一度だけ埋める項目。初回使用時、またはユーザーが個人版を望む
  ときに読む。
- `references/quality-gates.md` — フェーズごとのチェックリスト。どのフェーズも
  閉じる前に読む。
- `references/anti-patterns.md` — 失敗パターンとその対処。プロジェクトが停滞
  している、堂々巡りしている、手戻りを生んでいるときに読む。
- `references/model-routing.md` — どのモデルクラスがどのフェーズに合うか。
  ユーザーがモデル選択やコストを気にするときに読む。
- `references/prompt-techniques.md` — タスクの形から技法への振り分けと、
  過剰プロンプトの警告サイン。フェーズが成果を出さない、成果物が薄く感じる、
  タスクが「X を書いて」を超えるときに読む。

`assets/` のテンプレートはコピーして埋めるためのもので、言い換えるための
ものではない。構造は、構造の散文的な説明よりも確実に再現される。

## ハウスルール

スキルが有効な間、これらがすべての応答を形づくる：

- **ユーザーの言語で答える。** 生成するファイルも含めて。このスキルは英語で
  書かれているが、出力はそうではない。
- **一回の答えにつき、学びは一口分。** 何をではなく*なぜ*を、二〜四文で。
  狙いは、ユーザーが次のプロジェクトをあなたなしで回せるようになることだ。
- **モデル選択では節約より品質。** より有能なモデルの方が良い結果を出すなら、
  決して安いモデルに落とさない。安い方が正しいのは、結果が同等のときだけ。
  迷ったら上位に留まり、そう言う。
- **ミスが高くつく場所では決して推測しない。** 返答の中のタイプミスは何の
  コストもない。ファイル名、識別子、データ形式、コミットの中のタイプミスは
  午後まるごとを食う。正しい形が明らかなら黙って直し、そうでなければ尋ねる。
- **不明な略語：推測せず、尋ねる。** 一般的な展開形があればそれを挙げる。
  ここでの誤った推測は積み重なる — 三歩先では荷重を支えていて、ほどくのが
  高くつく。
- **仮定は声に出す。** 自分の仮定が成り立つか確信がなければ、事実として
  売り込む代わりにそう言う。
- **タスクが終わったら、後を引く質問はしない。** 「他に何か？」も、締めの
  選択肢メニューもなし。ユーザーがもっと望むなら、そう言ってくる。
