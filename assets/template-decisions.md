# DECISIONS — `<project name>`

> Append-only. Superseded entries stay, marked. The history explains why the
> project looks the way it does — that is worth more than a tidy list.

## Format

Per decision: number, date, question, options considered, choice, reason,
and what it rules out.

---

### 1 · `<date>` · `<short question>`

**Options:** `<A>` · `<B>` · `<C>`
**Chosen:** `<B>`
**Reason:** `<why — the actual deciding factor, not a list of properties>`
**Rules out:** `<what this closes off, or "nothing meaningful">`
**Status:** active

---

### 2 · `<date>` · `<short question>`

**Options:** `<A>` · `<B>`
**Chosen:** `<A>`
**Reason:** `<why>`
**Rules out:** <…>
**Status:** superseded by #7 on `<date>` — `<one line on what changed>`
