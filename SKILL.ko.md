# Create Masterprompt — 한국어판

> `SKILL.md`의 번역본이다. **프로그램이 읽는 것은 오직 영어 파일인
> `SKILL.md`뿐이며**, 이 파일은 사람이 읽어 보기 위한 것이다. 두 파일이
> 서로 어긋나면 영어 원본이 우선한다. 버전 1.7.0 기준.
>
> 파일 이름, 폴더, 예시 명령은 디스크에 그 이름 그대로 있기 때문에
> 일부러 번역하지 않았다.
>
> 한 장으로 훑어보는 개요: [`docs/uebersicht-ko.png`](docs/uebersicht-ko.png)
> (영어판: [`docs/uebersicht-en.png`](docs/uebersicht-en.png)).
>
> **상태: 초기 공개.** 본문은 내부 모순이 없는지, 스펙에 맞는지 여러 차례
> 검토했지만, 이 스킬 자체는 아직 다양한 실제 프로젝트에서 많이 쓰이지
> 않았다. 진행 흐름, 크기 게이트, 템플릿 가운데 자신의 작업 방식과 맞지
> 않는 부분이 있다면 그것은 유용한 신호다 — 이슈로 알려 주기 바란다.

마스터 프롬프트는 역할 프롬프트가 **아니다**. “너는 시니어 엔지니어다,
꼼꼼하게 일해라”는 유능한 모델이 이미 하고 있는 일에 아무것도 보태지
못한다. 마스터 프롬프트는 **컨텍스트 패키지**다. 한 프로젝트의 오래 가는
사실, 결정, 경계를, 대화 이력이 전혀 없는 세션이 지난 세션이 멈춘 바로
그 지점에서 이어서 일할 수 있도록 적어 둔 것이다.

이 스킬은 여섯 단계를 거쳐 그 패키지를 만들고 파일 세 개를 돌려준다.

## 산출물

| 파일 | 용도 | 수명 |
|---|---|---|
| `BRIEFING.md` | 마스터 프롬프트 본체. 컨텍스트, 결정, 안티스코프, 함정, 현재 상태. | 프로젝트 전체 |
| `DECISIONS.md` | 정리된 질문 하나당 한 줄, 이유와 함께. 덧붙이기만 하고 고치지 않는다. | 프로젝트 전체 |
| `HANDOFF_vNN.md` | 컨텍스트 한계 전에 쓴다. 새 세션이 *지금 당장* 필요로 하는 것. | 세션 하나 |

파일 이름은 사용자의 언어로 짓는다. 채팅이 아니라 작업물 옆에 둔다.

## 0단계 — 크기 게이트 (맨 먼저, 10초 안에)

이름 바꾸기 스크립트 하나에 여섯 단계를 다 돌리는 것이야말로 사람들이
절차 전체를 건너뛰는 법을 배우는 방식이다. 시작하기 전에 분류한다:

- **S — 한 번에 끝나고, 되돌릴 수 있고, 미지수가 없다.** 바로 작업으로
  넘어간다. 규모가 커지면 그때 전체 트랙을 제안한다.
- **M — 세션 몇 개, 미지수 약간, 진짜 갈림길 한두 개.** 1–3단계와 5단계,
  그리고 작업이 한 세션을 넘기는 즉시 6단계. 브리핑 파일은 만들되 짧게.
  별도의 계획 문서는 없다.
- **L — 여러 세션, 진짜 아키텍처, 되돌리기에 비용이 큰 결정.** 여섯 단계
  전부.

어느 크기를 골랐고 왜 그런지 한 문장으로 말한다. 사용자가 다르게 생각하면
그렇다고 말할 것이다 — 메시지 하나가 들고 한 시간을 아낀다.

## 1단계 — 리서치

이미 무엇이 있는지, 빈틈은 어디인지, 기술적으로 무엇이 가능한지, 어떤
함정이 이미 문서화되어 있는지 알아낸다. **리서치는 오직 여기서만 한다.**
빌드 도중의 리서치는 빌드가 토끼굴로 변하는 지름길이다.

산출물: 비교표, 권고안, 출처를 담은 보고서.

**다음 셋이 모두 참일 때 멈춘다** — 호기심이 바닥났을 때가 아니라:
1. 최종 후보 옵션에 대해 비교표에 빈 칸이 없다.
2. 함정 항목마다 출처가 있거나, 가정으로 표시되어 있다.
3. 마지막 두 번의 검색에서 새로운 것이 나오지 않았다. 그것이 포화다.

포화에 이르지 못하면 그렇다고 말하고 무엇이 열린 채 남았는지 밝힌다.
솔직한 빈틈이 자신만만한 추측보다 낫고, 시간을 더 쓸지는 사용자가 결정할
수 있다.

## 2단계 — 브리핑

`assets/template-briefing.md`를 써서 리서치 결과를 `BRIEFING.md`로
압축한다.

이 파일의 시험 기준: **이력이 없는 새 세션에 건넨다. 그것으로 일할 수
있는가?** 이미 알고 있던 것에 대해 확인 질문이 하나라도 필요하다면
브리핑은 불완전하다. 보여 주기 전에 적대적인 눈으로 다시 읽는다.

필수 섹션 — 앞의 둘은 사람들이 건너뛰고 나서 후회하는 것들이다:

- **안티스코프.** 명시적인 비목표, 각각 이유와 함께. “v1에는 암호화 없음 —
  볼트는 로컬 전용이고, 키 관리를 넣으면 빌드가 두 배가 된다.” 안티스코프는
  스코프 크리프에 맞서는 가장 강력한 방어책이다. “그냥 이것도 넣으면…”을
  공짜 추가가 아니라 다시 열어야 하는 결정으로 바꿔 놓기 때문이다.
- **가정 기록부.** 묻지 않고 결정한 모든 것. 한 줄씩, 나중에 이의를 제기할
  수 있도록 표시해 둔다. 기록되지 않은 가정은 비용이 커질 때까지 보이지
  않는다.
- 컨텍스트, 제약, 알려진 함정, 현재 상태.

## 3단계 — 결정 인터뷰

열린 결정들을 **한 번에 하나씩** 묻고, 다음 질문 전에 답을 기다린다.
묶어서 던진 질문은 대충 훑어지고, 대충 훑은 결정은 사용자의 서명을 두른
추측이다.

질문마다: 옵션 2–4개, 분명한 권고 하나, 그리고 그 이유. 의존 관계는
순서대로 푼다 — 스택을 먼저 정하고, 그 위에서 돌아갈 라이브러리는
그다음이다.

**직접 찾아본다.** 파일, 도구, 웹에서 알아낼 수 있는 사실이라면 직접
알아낸다. 진짜 선택만이 사용자의 몫이다.

**이 단계가 닫히기 전에는 아무것도 만들지 않는다.** 인터뷰 도중 사용자가
“그냥 시작해”라고 하면: 아직 열려 있는 결정을 구체적으로 짚고, 기록된
가정으로 직접 정하겠다고 제안하고, 사용자가 고른 뒤에야 계속한다. 갈림길이
열린 채로 시작하면 재작업이 생기고, 재작업은 인터뷰보다 비싸다.

모든 결정을 번호 매긴 요약으로 마무리한다. 이를
`assets/template-decisions.md`를 따르는 결정 로그에 덧붙인다.

## 4단계 — 계획

아키텍처, 저장소 또는 폴더 구조, 컨벤션 파일, 테스트 전략, 마일스톤,
마일스톤별 완료 정의, 그리고 프로젝트 전체의 완료 정의.

마일스톤은 **수직 슬라이스**다. 각각이 사용자가 실제로 실행하거나 보거나
쓸 수 있는 무언가를 내놓는다. 저마다 작동하는 조각을 내놓는 마일스톤 다섯
개가, 아무도 테스트할 수 없는 기반을 내놓는 셋보다 낫다.

빌드 전에 계획을 보여 주고 승인을 받는다.

## 5단계 — 빌드

**검증 가능한 종료 조건**이 있는 명시된 목표를 향해 일한다. “`npm test`가
통과하고 앱이 볼트 폴더를 열면 완료”는 검증할 수 있다. “잘 돌아가면
완료”는 그렇지 않다.

- 테스트가 의미 있는 곳에서는 기능보다 테스트를 먼저.
- 작은 커밋, 각각 단독으로 되돌릴 수 있게.
- 린트는 훅으로 건다. 잊지 말라는 당부로는 안 된다.
- 허락을 구하려고 멈추지 않는다. 진짜 설계 갈림길에서만 묻는다.

## 6단계 — 핸드오프

컨텍스트 한계에 닿기 전에 — 닿은 뒤가 아니라 —
`assets/template-handoff.md`를 바탕으로 핸드오프 파일(`HANDOFF_vNN.md`,
사용자의 언어로 이름 붙임)을 쓰고, 그런 다음 새 세션을 시작한다. 끝없는
압축은 확립하는 데 비용이 많이 든 바로 그 세부 사항을 잃어버린다.

트리거: 도구를 많이 쓰는 긴 구간, 같은 파일을 반복해서 다시 읽는 것, 또는
이미 다룬 내용을 사용자가 두 번째로 묻는 것.

## 개선 패스

단계 경계마다, 결과를 보여 주기 **전에**, 조용히 작성자에서 검토자로
전환한다. 질문 셋:

1. **무엇이 빠졌는가?** 이 파일이 답하지 않아서 새 세션이 물어야 할 질문은
   무엇인가?
2. **근거 없이 단언한 것은 무엇인가?** 출처나 가정 표시가 없는 주장은 모두
   후보다.
3. **군더더기는 무엇인가?** 다른 어떤 프로젝트에도 들어갈 수 있는 문장은
   뺀다.

사용자는 비평이 아니라 수정된 결과를 본다. 예외: 패스에서 결정과 맞닿는
것이 드러나면, 그것은 사용자 앞에 놓아야 한다.

만드는 것과 평가하는 것은 다른 활동이다. 작성자는 빈틈을 볼 수 없다. 빠진
조각이 자기 머릿속에 있기 때문이다. 단계의 대략 30 %가 들지만, 이 스킬의
다른 어떤 것보다 많이 돌려준다.

작업 형태가 “X를 써 줘”를 넘어서면, 먼저 `references/prompt-techniques.md`를
로드한다 — 작업 형태를 기법에 대응시키는 라우팅 표와 과잉 프롬프팅의 경고
신호가 거기 있다.

## 되감기 규칙

결정은 수정된다. 그것은 정상이고, **제대로 다루기만 하면** 비용이 싸다:

1. 결정 로그를 갱신한다 — 새 줄을 추가하고, 옛 줄은 대체됨으로 표시하고,
   둘 다 남긴다. 이력이 코드가 왜 지금 모습인지 설명해 준다.
2. `BRIEFING.md`를 갱신한다. 새 세션이 읽는 것이 바로 그 파일이기 때문이다.
3. 코드를 건드리기 전에 이 변경이 무엇을 무효로 만드는지 밝힌다.

채팅에서만 방향을 바꾸는 것이 실패 양상이다: 파일은 여전히 옛 프로젝트를
설명하고, 다음 세션은 그것을 믿고, 모순은 세 걸음 뒤에 드러난다.

## 종료 규칙

세 세션 동안 아무것도 내놓지 못한 프로젝트는 막혔거나 죽은 것이다.
그렇다고 분명히 말하고 선택지 셋을 제시한다: 범위를 줄이거나, 깔끔하게
재개할 수 있도록 문서화된 핸드오프와 함께 보류하거나, 접는다. 아이디어는
싸다. 반쯤 만든 프로젝트는 유지비가 든다. 여기가 직설이 곧 서비스인 유일한
곳이다.

## 주의할 점

합리적인 가정을 거스르는 환경상의 사실들. 증상을 우회하기 전에 이것부터
확인한다.

- **Anthropic 스킬 프론트매터는 정확히 여섯 개의 키만 받는다**: `name`,
  `description`, `license`, `allowed-tools`, `metadata`, `compatibility`. 그 밖의
  키는 Claude Code에서는 허용되더라도 claude.ai 업로드 시 검증에 실패한다.
  스킬이 로컬에서는 조용히 잘 돌다가 업로드에서 깨진다.
- **폴더 이름은 NFKC 정규화 후 `name` 필드와 같아야 한다.** 프론트매터를
  함께 고치지 않고 폴더 이름만 바꾸는 것이 가장 흔한 고장 원인이다.
- **`name`: 소문자, 숫자, 하이픈만.** 밑줄 금지, 연속 하이픈 금지, 맨 앞이나
  맨 뒤의 하이픈 금지. 최대 64자. `description`은 최대 1024자.
- **`description` 안의 따옴표 없는 콜론은 YAML 파싱을 깨뜨린다.** “Use
  when: …”은 실패한다. 따옴표로 감싸거나 블록 스칼라(`>-`)를 쓴다.
- **세션 시작 시에는 `name`과 `description`만 로드된다.** 본문은 트리거될
  때 로드된다. 따라서 “언제 쓰는가”에 관한 힌트는 전부 description에
  들어가야 한다. 본문에 묻힌 트리거 조건은 트리거할 시점에 절대 읽히지
  않는다.
- **단순한 한 단계 요청은 스킬을 트리거하지 않는다.** description이 아무리
  좋아도 마찬가지인데, 모델이 직접 처리하기 때문이다. 트리거는 내용이 있는
  여러 단계짜리 프롬프트로 테스트한다.
- **스킬은 지시이지 강제가 아니다.** `allowed-tools`는 권한 확인 프롬프트를
  면제할 뿐, 아무것도 제한하지 않는다.

## 참고 파일

단계가 요구할 때 로드한다. 미리 로드하지 않는다:

- `references/profile-questionnaire.md` — 사용자가 한 번 채워 넣어 이 스킬을
  자기 것으로 만드는 항목들. 처음 쓸 때, 또는 사용자가 개인화된 변형을 원할
  때 읽는다.
- `references/quality-gates.md` — 단계별 체크리스트. 어떤 단계든 닫기 전에
  읽는다.
- `references/anti-patterns.md` — 실패 양상과 그 해결책. 프로젝트가
  정체되거나, 맴돌거나, 재작업을 낳을 때 읽는다.
- `references/model-routing.md` — 어느 모델 등급이 어느 단계에 맞는지.
  사용자가 모델 선택이나 비용에 신경 쓸 때 읽는다.
- `references/prompt-techniques.md` — 작업 형태에서 기법으로의 라우팅, 그리고
  과잉 프롬프팅의 경고 신호. 어떤 단계가 결과를 내지 못하거나, 산출물이
  빈약해 보이거나, 작업이 “X를 써 줘”를 넘어설 때 읽는다.

`assets/`의 템플릿은 복사해서 채우라고 있는 것이지, 바꿔 쓰라고 있는 것이
아니다. 구조는 구조에 대한 산문 설명보다 더 확실하게 맞춰진다.

## 기본 규칙

이 스킬이 활성화되어 있는 동안 모든 응답의 모양을 정한다:

- **사용자의 언어로 답한다.** 생성되는 파일도 마찬가지다. 이 스킬은 영어로
  쓰였지만, 출력은 그렇지 않다.
- **답변 하나에 배움 한 조각.** 무엇이 아니라 *왜*에 대해 두 문장에서 네
  문장. 요점은 사용자가 다음 프로젝트를 혼자 힘으로 돌릴 수 있게 되는
  것이다.
- **모델 선택에서는 절약보다 품질.** 더 유능한 모델이 더 나은 결과를 내는데
  절대 더 싼 모델로 내려가지 않는다. 결과가 동등할 때만 더 싼 쪽이 옳다.
  확신이 없으면 상위 모델에 머물고, 그렇게 말한다.
- **실수가 비싼 곳에서는 절대 추측하지 않는다.** 답변 속 오타는 비용이 없다.
  파일 이름, 식별자, 데이터 형식, 커밋 속 오타는 오후 한나절을 잡아먹는다.
  올바른 형태가 분명하면 조용히 고치고, 아니면 묻는다.
- **불분명한 약어: 추측하지 말고 묻는다.** 통용되는 풀이가 있으면 그것을
  언급한다. 여기서의 잘못된 추측은 눈덩이처럼 불어난다 — 세 걸음 뒤에는
  하중을 받치고 있어서 되돌리기에 비싸다.
- **가정은 소리 내어 말한다.** 가정이 성립하는지 확신이 없으면, 사실인 양
  팔지 말고 그렇다고 말한다.
- **작업이 끝나면 꼬리 질문은 없다.** “더 필요한 게 있나요?”도, 마무리
  옵션 메뉴도 없다. 사용자가 더 원하면 말할 것이다.
